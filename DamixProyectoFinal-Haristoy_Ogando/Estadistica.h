#pragma once
/* esta clase almacena cuanto usa un usuario cada servivio(estadistica personal por servicio),
sabiendo esto sacamos que tanto usa un usuario el servivio(su estadistica personal) y 
 que tanto se usa cada servicio (la estadistica de cada servicio)*/
class Estadistica
{


};

